#include <stdio.h>
#include <stdlib.h>

int main() { 

int nota1, nota2, nota3, soma;
float media;

soma = nota1 + nota2 + nota3;
media = soma / 3.0;

printf("A média é: %.2f\n", media);

    return 0;
}
